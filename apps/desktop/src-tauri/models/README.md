This directory is packaged into the Tauri bundle as resources.

Place prebundled faster-whisper CTranslate2 model folders here:

- `models/large-v3/`
- `models/distil-large-v3/`

Use the scripts in `scripts/` to download these model repos into this folder.

